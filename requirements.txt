# Frontend-only dependencies (Streamlit Cloud)
# The heavy ML/CV/NLP stack runs in the separate FastAPI backend (requirements-api.txt)
streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2
requests==2.31.0
streamlit-lottie==0.0.5
